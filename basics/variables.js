// 1. Cant' define a variable more than once

let petName = 'Hal'
petName = 'Spot'

petName //

// 2. There are rules to variable names
// must start with letter, $, or underscore

// 3. can't be reserved keyword
// like "let"
